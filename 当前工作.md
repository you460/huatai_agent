# 当前工作

项目目标：用户输入自然语言问题后，系统能找到正确数据、生成安全可执行的 SQL，并在网页中给出结果。评测通过只是基础，代码还要清楚、能演示、别人能复现。

## A：核心逻辑、正确性、最终验收

1. 跑全量 150 题评测。

   ```powershell
   Remove-Item Env:QUESTION_IDS -ErrorAction SilentlyContinue
   python evaluation/evaluate.py
   ```

2. 有失败题时，只看这道题的题目、模型 SQL、标准 SQL 和结果差异。判断是提示规则、标准答案还是评测规则的问题；只改直接相关的地方，先复测该题，再重新跑全量。

3. 保证核心代码好读、逻辑正确。

   - `main.py`：规则集中、互不矛盾；不要为单题写死 SQL。
   - `security_guard.py`：生成 SQL 仍必须经过安全检查，不能为了通过题目放宽危险操作。
   - `evaluation/`：题库、标准答案、评测逻辑要对应；改题库时同步改 `build_questions.py`，避免重新生成后丢失修改。

4. 全量通过后，检查没有临时打印、无用测试代码或写死密钥；然后本地 Git 提交，网络恢复后再推送。

完成标准：150 题通过（或每个失败题都有明确原因）；日期、客户筛选、持仓、交易、资金流、机构层级、产品分类、排名、占比等常见逻辑稳定。

## B：网页功能、说明、演示与 GitHub

1. 检查和完善 `app.py`。

   让用户能输入问题、点击查询、看到 SQL 和结果；失败时要有清楚提示，页面不能卡住。不要改 SQL 生成逻辑。

2. 完善 `README.md`。

   写清项目用途、依赖和 `.env` 配置方式、网页启动命令、单题/全量评测命令、主要目录用途。不能把真实密钥写进 README 或提交到 GitHub。

3. 准备比赛演示。

   准备 2 到 3 个代表问题，截取“提问 → SQL → 结果”的画面；再保存全量评测结果图，准备简短讲解。题目可各选一类：客户统计、复杂交易筛选、持仓资产综合查询。

完成标准：新同学按 README 能启动项目和跑评测；网页能完整演示至少 3 题；仓库提交记录清楚。

## 协作边界

- A 负责：`main.py`、`security_guard.py`、`metadata_tools.py`、`evaluation/` 中的题库和评测逻辑。
- B 负责：`app.py`、`README.md`、演示材料、GitHub 同步。
- B 暂时不改 A 的核心逻辑；A 也尽量不改网页样式和 README。
- 要跨范围改文件，先说明改哪个文件、为什么改、是否影响对方。
- 每次提交前运行相关测试，提交信息写清做了什么。
